[package]
name = "kangnam-design-prompt-template"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Catalog of ready-made image / video generation prompt templates (≥94 vendored templates: profile-avatar, social-media, cinematic, hyperframes, …). Typed loader + filters by surface / category / tag / model. Distinct from `kangnam-design-prompt` (which composes system prompts)."
keywords = ["prompt", "template", "image", "video", "generation"]
categories = ["multimedia", "data-structures"]

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
