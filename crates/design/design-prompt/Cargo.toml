[package]
name = "design-prompt"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "System-prompt composer for the design family — stacks DISCOVERY directives + identity charter + active DESIGN.md + active SKILL.md + project metadata into a single system prompt string."
keywords = ["prompt", "design", "system-prompt", "composer"]
categories = ["multimedia"]

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
