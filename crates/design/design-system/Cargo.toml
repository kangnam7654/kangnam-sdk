[package]
name = "design-system"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "DESIGN.md catalog — 9-section design-system parser + token extractor (#hex / oklch / rgb), built around the awesome-design-md schema."
keywords = ["design", "design-system", "tokens", "palette"]
categories = ["multimedia"]

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
