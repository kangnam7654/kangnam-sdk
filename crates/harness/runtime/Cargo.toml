[package]
name = "kangnam-harness-runtime"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Domain-agnostic AI agent tool execution runtime — AgentTool trait, ToolCtx, suspend/resume via InteractionBridge, permission + hook evaluation."

[dependencies]
kangnam-harness-core = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
async-trait = { workspace = true }
tokio = { workspace = true, features = ["sync", "macros", "rt", "process", "time"] }
uuid = { workspace = true }

[dev-dependencies]
tokio = { workspace = true, features = ["macros", "rt", "sync", "process", "time"] }
