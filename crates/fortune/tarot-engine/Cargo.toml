[package]
name = "tarot-engine"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Tarot card fortune engine — Rider-Waite Major Arcana deck, spread drawing, per-card interpretation. Pure computation, no IO."

[lints.rust]
unsafe_code = "deny"

[dependencies]
chrono = { workspace = true }
rand = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
sha2 = { workspace = true }

[dev-dependencies]
# Tests are inline `#[cfg(test)]` modules — no extra dev deps.
